# ============================================================================
# Salina ERP - Environment Variables
# ============================================================================
# Copy this file to .env.local for local development
# For production deployment, set these in your deployment platform (Railway/Fly.io/Render)

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
# Local dev: postgresql://postgres:postgres@localhost:5432/salina_dev
# Production: Use connection pooling URL from your database provider
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/salina_dev

# ============================================================================
# CLERK AUTHENTICATION
# ============================================================================
# Get these values from https://dashboard.clerk.com
# Create separate applications for development and production

# Publishable key (safe to expose to client)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Secret key (NEVER commit or expose to client)
CLERK_SECRET_KEY=sk_test_your_secret_key_here

# Webhook secret for verifying Clerk webhook signatures
CLERK_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Clerk redirect URLs (customize per environment)
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/

# ============================================================================
# REDIS CONFIGURATION (Optional for future caching)
# ============================================================================
REDIS_URL=redis://localhost:6379

# ============================================================================
# NEXT.JS CONFIGURATION
# ============================================================================
# Node environment (development|production|test)
NODE_ENV=development

# ============================================================================
# SENTRY ERROR TRACKING (Production Only)
# ============================================================================
# Get these from https://sentry.io
# Server-side Sentry DSN
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
# Client-side Sentry DSN (safe to expose)
# NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
# Build-time auth token (for source maps)
# SENTRY_AUTH_TOKEN=your-sentry-auth-token
# SENTRY_ORG=your-org-name
# SENTRY_PROJECT=your-project-name

# ============================================================================
# DEPLOYMENT PLATFORM (Production Only)
# ============================================================================
# Railway/Fly.io/Render automatically set these variables
# No manual configuration needed for:
# - PORT (automatically set by platform)
# - HOSTNAME (automatically set to 0.0.0.0)
# - DATABASE_URL (use platform's connection pooler)
